//

const a = 'hello';
// a.includes()
const b = 123;
const c = true;

const d = ['cpffee', 'bread', 'cream'];
d.map((v) => {
  v + 10;
});

const e = document.createElement('div');
e.appendChild();
const f = { name: 'kim', age: 21 };
f.keys();

// type-casting
// explicit : Number(),String(),Boolean,Array(),Object()
// implicit : +, !!, [], {},
