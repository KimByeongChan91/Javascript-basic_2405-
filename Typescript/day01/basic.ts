const b: string = 'hello world';
const b1: number = 100;
// boolean, null, undefined
const c: boolean = false;
const d: null = null;
const e: undefined = undefined;

// object {}

const f: object = { name: '아이스아메', price: 2500 };
const g: {} = { name: '라떼', price: 3000 };

// 타입 추론
// array
const i: Number[] = [1, 2, 3, 4, 5];

// 최후의 보루임 (모든타입)
const j:any = "하고싶은 대로해"
